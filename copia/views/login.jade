extends ./layout.jade
block contenido
	div(class="col-md-3 remove-float center-block big-top-space")
		form(action="/sessions" method="POST")
			div(class="form-group")
				label(for="email") Email
				input(type="text" name="email" placeholder="usuario" id="email" class="form-control")
			div
				label(for="password") password
				input(type="password" name="password" placeholder="contraceña" id="password" class="form-control")
			div(class="top-space")
				input(type="submit" value="Iniciar Sesion" class="btn btn-info")